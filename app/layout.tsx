import type { Metadata } from "next";
import { Space_Grotesk } from "next/font/google";
import "./globals.css";

const spaceGrotesk = Space_Grotesk({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  variable: "--font-space-grotesk",
});

export const metadata: Metadata = {
  title: "Vibe Directory — Projects Built on Pure Vibes",
  description: "A curated showcase of independently built digital experiences. Discover projects created by passionate makers building with intention.",
  openGraph: {
    title: "Vibe Directory — Projects Built on Pure Vibes",
    description: "A curated showcase of independently built digital experiences",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={spaceGrotesk.className}>{children}</body>
    </html>
  );
}
